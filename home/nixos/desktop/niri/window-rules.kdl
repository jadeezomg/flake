// Window rules for Niri
// These rules apply to all windows matching the specified criteria

// Video player opaque
window-rule {
    match app-id=r#"^org.gnome.Showtime$"#
    opacity 1.0
}

// Set Cursor and Zed IDE to full width
window-rule {
    match app-id=r#"^cursor$"#
    match app-id=r#"^dev\.zed\.Zed$"#

    default-column-width { proportion 1.0; }
}

// Example: block out two password managers from screen capture.
// (This example rule is commented out with a "/-" in front.)
window-rule {
    match app-id=r#"^org\.keepassxc\.KeePassXC$"#
    match app-id=r#"^org\.gnome\.World\.Secrets$"#
    match app-id=r#"^Proton Pass$"#

    block-out-from "screen-capture"

    open-floating true
    default-window-height { proportion 0.75; }
    default-column-width { proportion 0.3; }
    default-floating-position x=100 y=100 relative-to="top-right"

    opacity 0.90
    background-effect {
        blur true
    }
}

// Recommended window rules for DMS integration
// GNOME applications
window-rule {
    match app-id=r#"^org\.gnome\."#
    draw-border-with-background false
    geometry-corner-radius 12
    clip-to-geometry true
}

// Terminal applications
window-rule {
    match app-id=r#"^org\.wezfurlong\.wezterm$"#
    match app-id="Alacritty"
    match app-id="zen-beta"
    match app-id="zen-twilight"
    match app-id="com.mitchellh.ghostty"
    match app-id="kitty"
    
    default-column-width { proportion 0.4; }
}

layer-rule {
    match namespace="dms:bar"
    // opacity 0.95

    // background-effect {
    //     blur true  
    //     // xray false
    // }
}

window-rule {
    match app-id="zen-beta"
    match app-id="cursor"
    opacity 0.95

    background-effect {
        blur true
    }
}

window-rule {
    match app-id="com.github.th_ch.youtube_music"
    opacity 0.80

    background-effect {
        blur true
    }
}
    

// Open DMS windows (quickshell) as floating by default
window-rule {
    match app-id=r#"org.quickshell$"#
    open-floating true
}

