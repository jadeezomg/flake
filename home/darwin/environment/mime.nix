{...}: {
  # Darwin/macOS MIME configuration
  # Note: macOS uses LaunchServices rather than XDG MIME for associations
  # xdg.mimeApps is not supported on Darwin - use macOS native tools instead
  # Use `duti` or System Preferences to set default applications
}
